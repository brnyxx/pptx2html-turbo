# Task 17 effects and 3D evidence

Evidence policy:

- Keep prior Task 17 artifacts for historical context.
- For each review regression, RED and GREEN logs must record the identical focused command on their first line.
- RED output must be captured from a genuine failing run before the implementation fix; GREEN output must be captured after the fix.
- Screenshots and diagnostic JSON must come from the real CLI conversion surface, not a mocked renderer.

The descendant/attribute namespace regression is recorded in `task-17-effects-3d-namespace-red.txt` and `task-17-effects-3d-namespace-green.txt`.
