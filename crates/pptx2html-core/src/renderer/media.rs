use std::cell::RefCell;

use super::UnresolvedCollector;
use crate::model::Presentation;

pub(super) fn append_diagnostics(
    _presentation: &Presentation,
    _collector: &RefCell<UnresolvedCollector>,
) {
}
